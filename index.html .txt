<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy 18th Birthday Mahi 🤍</title>

<style>
*{box-sizing:border-box;}

body{
  margin:0;
  height:100vh;
  background:linear-gradient(135deg,#ffe6f0,#fff);
  display:flex;
  justify-content:center;
  align-items:center;
  font-family:'Poppins',sans-serif;
  overflow:hidden;
}

/* Card */
.card{
  background:white;
  padding:24px;
  border-radius:25px;
  text-align:center;
  width:92%;
  max-width:360px;
  box-shadow:0 15px 40px rgba(0,0,0,0.15);
  animation:pop 0.9s ease;
  z-index:2;
}

@keyframes pop{
  0%{transform:scale(0.6);opacity:0;}
  100%{transform:scale(1);opacity:1;}
}

.heart{
  font-size:40px;
  animation:beat 1s infinite;
}

@keyframes beat{
  0%,100%{transform:scale(1);}
  50%{transform:scale(1.25);}
}

h1{
  color:#ff4d88;
  font-size:24px;
  margin:8px 0 0;
}

h2{
  color:#666;
  font-size:18px;
  margin:4px 0 10px;
}

p{
  font-size:14px;
  color:#444;
  line-height:1.5;
}

.countdown{
  margin-top:10px;
  font-size:13px;
  color:#777;
}

button{
  margin-top:15px;
  padding:10px 24px;
  border:none;
  border-radius:25px;
  background:#ff4d88;
  color:white;
  font-size:14px;
  cursor:pointer;
  transition:0.3s;
}

button:hover{
  background:#e63e77;
}

/* Floating Hearts */
.floating-heart{
  position:absolute;
  bottom:-20px;
  font-size:20px;
  color:rgba(255,255,255,0.85);
  animation:floatUp linear infinite;
}

@keyframes floatUp{
  0%{transform:translateY(0);opacity:1;}
  100%{transform:translateY(-110vh);opacity:0;}
}

/* Confetti */
.confetti{
  position:absolute;
  width:8px;
  height:8px;
  background:#ff4d88;
  animation:confettiFall 2.5s linear forwards;
}

@keyframes confettiFall{
  0%{transform:translateY(-20px) rotate(0);}
  100%{transform:translateY(110vh) rotate(720deg);}
}
</style>
</head>

<body>

<!-- Music -->
<audio id="bgMusic" loop>
<source src="https://cdn.pixabay.com/audio/2022/10/30/audio_5b1bff64a1.mp3" type="audio/mpeg">
</audio>

<div class="card">
  <div class="heart">🤍</div>
  <h1>Happy 18th Birthday</h1>
  <h2>Mahi ✨</h2>

  <p>
    You’re officially 18 now 🎉  
    Adult by age, cute by default 😌  
    <br><br>
    May this year bring you happiness, success,
    endless smiles, and cake (very important).  
    <br><br>
    Stay amazing, stay kind,
    and never forget how special you are 🤍
  </p>

  <div class="countdown" id="countdown"></div>

  <button onclick="startSurprise()">Tap for Surprise 🎁</button>
</div>

<script>
/* Start surprise */
function startSurprise(){
  document.getElementById("bgMusic").play();
  launchConfetti();
  alert("Welcome to adulthood 😌🤍\nMentally optional.\nTreat still compulsory ☝🏻");
}

/* Floating hearts */
function createHeart(){
  const heart=document.createElement("div");
  heart.className="floating-heart";
  heart.innerHTML="🤍";
  heart.style.left=Math.random()*100+"vw";
  heart.style.animationDuration=(4+Math.random()*3)+"s";
  document.body.appendChild(heart);
  setTimeout(()=>heart.remove(),7000);
}
setInterval(createHeart,400);

/* Confetti */
function launchConfetti(){
  for(let i=0;i<80;i++){
    const conf=document.createElement("div");
    conf.className="confetti";
    conf.style.left=Math.random()*100+"vw";
    conf.style.background=["#ff4d88","#ffd1e6","#fff"][Math.floor(Math.random()*3)];
    conf.style.animationDuration=(2+Math.random()*2)+"s";
    document.body.appendChild(conf);
    setTimeout(()=>conf.remove(),4000);
  }
}

/* Countdown (to today = birthday memory) */
const today=new Date();
document.getElementById("countdown").innerHTML=
"🎂 18 looks good on you — enjoy your day!";
</script>

</body>
</html>